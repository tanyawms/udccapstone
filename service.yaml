apiVersion: v1
kind: Service
metadata: 
  name: nginx
  labels: 
    name: nginx
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  selector: 
    name: nginx
    version: "1.10"
  type: LoadBalancer