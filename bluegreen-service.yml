apiVersion: v1
kind: Service
metadata:
  app: green
  labels:
    name: green
spec:
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  selector:
    app: green
  type: LoadBalancer 
